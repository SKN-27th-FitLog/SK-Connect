[pytest]
testpaths = tests
pythonpath = .
markers =
    readonly_smoke: optional DB read-only checks; merge still blocked
    slow: downloads HF model; excluded by default
    merge_mock: allows real merge_analysis_data when PostgreDB is mocked
addopts = -m "not readonly_smoke and not slow"
