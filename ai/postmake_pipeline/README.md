1. from_crawling
2. data_preprocessing
3. prompt
4. llm
5. validator
6. to_post
7. run

- utils (log, )

---

1. 데이터 조회: posts에 없는 crawling 데이터만 조회
  - 주의사항: crawling_id 기준 중복 방지 필수, 대량 데이터 시 batch limit 적용 필요
2. 데이터 없음 체크: 처리 대상 없으면 종료
  - 주의사항: 반드시 로그 기록, 불필요한 LLM 호출 방지
3. 배치 사이즈 제한: 한 번에 처리할 개수 제한
  - 주의사항: LLM 비용/장애 대비 (예: 100개 단위), 전체 한 번에 처리 금지
4. 전처리: HTML 제거, 공백 정리
  - 주의사항: 텍스트 손상 주의, 과도한 정규식 사용 금지
5. 입력 길이 제한: 너무 긴 데이터 컷
  - 주의사항: 토큰 초과 방지 (요약 전에 반드시 적용)
6. 요약 단계: LLM으로 핵심 정보 추출
  - 주의사항: retry 필요, 실패 시 fallback 처리
7. 구조화된 입력 생성: 제목/카테고리/위치/요약 구성
  - 주의사항: 필드명 고정, null 대신 "없음" 사용
8. 스타일 선택: 제한된 스타일 내 랜덤 선택
  - 주의사항: 완전 랜덤 금지, 서비스 톤 유지
9. few-shot 적용: 예시 기반 생성 유도
  - 주의사항: 예시 품질 = 결과 품질
10. 게시글 생성: LLM 호출
  - 주의사항: 카테고리는 고정값 사용, LLM에게 판단 맡기지 않음
11. 출력 형식 강제: JSON(title, content)
  - 주의사항: JSON 외 텍스트 금지하도록 프롬프트 설계
12. JSON 파싱: 결과 파싱
  - 주의사항: 실패 대비 regex fallback 필수, None 체크
13. 결과 검증: 길이/금지어/형식 체크
  - 주의사항: LLM 결과 신뢰 금지, 검증 통과만 저장
14. 중복 콘텐츠 검사: 유사 제목/내용 필터링
  - 주의사항: 동일 콘텐츠 반복 생성 방지
15. 재시도 처리: 실패 시 retry
  - 주의사항: 최대 2~3회 제한, 무한 루프 금지
16. fallback 처리: 반복 실패 시 대체 로직
  - 주의사항: 최소한의 content라도 생성하거나 skip 결정 필요
17. 저장: posts 테이블 insert
  - 주의사항: crawling_id 유지, 트랜잭션 고려
18. 예외 처리: 개별 row 실패 시 continue
  - 주의사항: 전체 배치 중단 방지
19. 로그 기록: 처리 상태 기록
  - 주의사항: 성공/실패/건수 모두 기록, 디버깅 가능하게
20. 실행 환경 관리: 환경 변수 및 API 키 관리
  - 주의사항: 코드에 키 하드코딩 금지 (.env 사용)
21. rate limit 대응: LLM 호출 속도 제어
  - 주의사항: sleep 또는 queue 필요
22. 실행: Django management command
  - 주의사항: 로컬/서버 동일 실행 가능해야 함
23. 스케줄링: cron 주 1회 실행
  - 주의사항: 중복 실행 방지 (lock 또는 실행 시간 관리)
24. 전체 원칙: LLM은 생성만, 검증은 코드
  - 주의사항: 역할 분리 유지
25. 품질 흐름: 요약 → 구조화 → few-shot → 스타일 → 검증
  - 주의사항: 순서 변경 금지
26. 최종 목표: idempotent + 안정적 배치 시스템
  - 주의사항: 동일 데이터 재실행 시 중복 생성 절대 금지

