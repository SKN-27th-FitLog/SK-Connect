[pytest]
testpaths = tests
pythonpath = .
markers =
    readonly_smoke: optional DB read-only checks; merge still blocked
    slow: long-running or sleep-heavy tests; excluded by default
    merge_mock: allows insert_crawling_batch when PostgreDB is mocked
addopts = -m "not readonly_smoke and not slow"
