
---

![[Pasted image 20260408190858.png]]


# Naive RAG
| 단계                      | 설명                                 |
| ----------------------- | ---------------------------------- |
| User / Query            | 사용자가 자연어로 질문을 입력하는 단계              |
| ↓                       | 흐름 진행                              |
| Documents               | 사전에 수집된 원본 문서 데이터 (PDF, DB, 크롤링 등) |
| ↓                       | 문서를 처리하기 위한 준비 단계                  |
| Document Chunks         | 문서를 작은 단위로 분할하여 검색 효율을 높임          |
| ↓                       | 벡터화 과정으로 이동                        |
| Vector Database         | 문서 조각을 임베딩하여 벡터 형태로 저장             |
| ↓                       | 유사도 검색 수행                          |
| Related Document Chunks | 사용자 질문과 유사한 문서 조각을 검색              |
| ↓                       | LLM 입력 준비                          |
| Prompt → LLM            | 검색된 문서를 포함해 프롬프트 생성 후 LLM에 전달      |

# Naive RAG with LangChain LCEL

1. 데이터 로딩 및 전처리
	1. 데이터 로딩
	2. 텍스트 분할기
2. 벡터 저장소 구축
	1. 임베딩 모델 설정
	2. 벡터 저장소 생성
3.  검색 테스트


### 1-1 데이터 로딩
```python
from langchain_community.document_loaders import TextLoader

loader = TextLoader('test.txt', encoding = 'utf-8')
documents = loader.load()
```

### 1-2 텍스트 분할기 설정
```python
from langchain_text_splitters import RecursiveCharacterTextSplitter

text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=250,      # 각 청크의 최대 크기
    chunk_overlap=50,    # 청크 간 겹치는 부분
    length_function=len,
    separators=["\n\n", "\n", " ", ""]
)

# 문서를 청크로 분할
texts = text_splitter.split_documents(documents)
print(f"분할된 청크 수: {len(texts)}")
```

### 2-1 임베딩 모델 설정
```python
from langchain_ollama import OllamaEmbeddings

embeddings = OllamaEmbeddings(model = '모델명')
```

### 2-2 벡터 저장소 생성
```python
from langchain_community.vectorstores import Chroma

vectorstore = Chroma.from_documents(
	documents = texts,
	embedding = embeddings,
	collection_name = 'rag-keywords',
	persist_directory = './chroma_db'
)
```

### 3. 검색 테스트
```python
test_queries = [
    "LLM란 무엇인가요?",
    "벡터 데이터베이스의 역할은?",
    "임베딩은 어떻게 생성되나요?"
]

  
for query in test_queries:
    print(f"\n질문: {query}")
    
    results = vectorstore.similarity_search(
        query,  # 질문
        k=1     # 유사한 데이터(청크) 조회할 횟수
    )

    print(f"답변: {results[0].page_content[:150]}...")
    print("="*50)
```

# Naive RAG with LangChain LCEL

1. VectorDB 로드
	1. Embedding Model
	2. VectorDB
2. LangChain LCEL을 이용한 RAG 체인 구성
	1. LLM 모델 설정
	2. 검색기 설정
	3. LCEL을 이용한

### VectorDB로드
```python

```